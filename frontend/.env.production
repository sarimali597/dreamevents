# Production API base (Render backend). Vite inlines this at build time.
# If your Render backend URL differs, update it here (or set VITE_API_URL in Vercel dashboard).
VITE_API_URL=https://dreamevents-backend.onrender.com/api/v1
